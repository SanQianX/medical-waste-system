<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.waste.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.hospital.waste.entity.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="department_id" property="departmentId"/>
        <result column="role_id" property="roleId"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_user ORDER BY create_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE username = #{username}
    </select>

    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE status = #{status}
    </select>

    <select id="selectByDepartmentId" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE department_id = #{departmentId}
    </select>

    <select id="selectByRoleId" resultMap="BaseResultMap">
        SELECT * FROM sys_user WHERE role_id = #{roleId}
    </select>

    <insert id="insert" parameterType="com.hospital.waste.entity.SysUser" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, real_name, department_id, role_id, phone, status, create_time, update_time)
        VALUES (#{username}, #{password}, #{realName}, #{departmentId}, #{roleId}, #{phone}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.hospital.waste.entity.SysUser">
        UPDATE sys_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="departmentId != null">department_id = #{departmentId},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>

</mapper>
